apiVersion: v1
kind: Pod
metadata:
  name: prisma-defender
  namespace : platform-security
spec:
  containers:
    - command:
        - sleep
        - "3600"
      image: busybox
      name: prisma-policy